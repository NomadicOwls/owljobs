---
export interface Props { class?: string; [key: string]: unknown; }
const { class: className = "", ...rest } = Astro.props;
---
<div class:list={["flex items-center px-6 pb-5", className]} {...rest}><slot /></div>
