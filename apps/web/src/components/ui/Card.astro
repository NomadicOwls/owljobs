---
export interface Props {
  class?: string;
  [key: string]: unknown;
}
const { class: className = "", ...rest } = Astro.props;
---
<div class:list={["bg-card text-card-foreground flex flex-col rounded-xl border shadow-sm", className]} {...rest}>
  <slot />
</div>
