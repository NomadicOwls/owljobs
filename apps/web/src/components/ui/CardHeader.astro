---
export interface Props { class?: string; [key: string]: unknown; }
const { class: className = "", ...rest } = Astro.props;
---
<div class:list={["flex flex-col gap-1.5 px-6 py-5", className]} {...rest}><slot /></div>
