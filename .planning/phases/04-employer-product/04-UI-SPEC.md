---
phase: 4
slug: employer-product
status: draft
shadcn_initialized: false
preset: hand-rolled shadcn-compatible (Geist + oklch CSS vars + niche-injected accent)
created: 2026-05-12
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4 (Employer Product). Generated by gsd-ui-researcher, verified by gsd-ui-checker.

Phase 4 ships five surfaces:
1. Public `/employers/[slug]` profile page (extend existing) — add **logo (logo.dev)** and **Claim this listing** CTA.
2. Claim form (modal on the same page) — collects work email, triggers domain check + magic link.
3. Magic-link auth pages — `/login`, `/auth/callback`, `/auth/check-email`.
4. Employer dashboard `/dashboard` — profile view card, featured-job toggles, 30-day analytics, locked profile editor preview, locked logo upload, weekly subscriber-count card.
5. SEO landing pages `/[niche-keyword]-jobs-[location-or-modifier]` — auto-generated intro + filtered job list, reuse `JobCardModern`.

All copy is English-only (per Out of Scope), multi-niche safe (no `wind_turbine` hardcoding — niche name derived from `niche.name`).

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (hand-rolled, shadcn-compatible patterns) |
| Preset | not applicable — `apps/web/tailwind.config.mjs` already wires oklch CSS variables to shadcn token names (`--background`, `--foreground`, `--card`, `--primary`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, `--radius`) |
| Component library | none (Astro components in `apps/web/src/components/ui/`: `Button`, `Card`, `CardHeader`, `CardContent`, `CardFooter`, `Badge`, `Checkbox`, `Input`, `ThemeToggle`) |
| Icon library | inline SVG (24×24, `stroke-width="2"`, `viewBox="0 0 24 24"`, currentColor) — matches existing site convention; no icon dependency added |
| Font | Geist Variable (sans) + Geist Mono Variable, loaded via `@fontsource-variable/geist*` in `apps/web/src/styles/global.css` |
| Theme | light + dark, class-toggled (`darkMode: "class"`), pre-paint no-flash script in `Layout.astro` is reused as-is |
| Niche brand override | `<html style="--accent:${niche.branding.accentColor};--ring:${niche.branding.accentColor};--primary:${niche.branding.primaryColor};">` — Phase 4 must NOT hardcode wind-turbine `#2ecc71`; all accent usage flows through the `--accent` CSS variable so `niche.branding` controls it. |

**Reuse mandate:** Every UI surface in this phase composes the existing `Card`, `Button`, `Badge`, `Input`, `Checkbox`, `JobCardModern`, `FeaturedJobCard`, `Layout`, `Header`, `Footer` components. New components are ADDITIVE (dashboard-specific) — they must not redefine spacing, type, or color tokens.

---

## Spacing Scale

Tailwind default scale, restricted to multiples of 4 for this phase.

| Token | Value | Tailwind | Usage |
|-------|-------|----------|-------|
| xs | 4px | `1` / `gap-1` / `p-1` | Icon-to-label gap, inline tag padding |
| sm | 8px | `2` / `gap-2` / `p-2` | Compact element spacing, badge padding-y |
| md | 16px | `4` / `gap-4` / `p-4` / `mb-4` | Default card padding, list item gap |
| lg | 24px | `6` / `p-6` / `mb-6` | Section padding, card internal padding (matches `/employers/[slug]` header card) |
| xl | 32px | `8` / `py-8` / `mb-8` | Container vertical rhythm, dashboard section spacing |
| 2xl | 48px | `12` / `py-12` | Hero/landing-page intro section |
| 3xl | 64px | `16` / `py-16` | Empty-state vertical padding (matches existing `py-16` empty states) |

Exceptions:
- Page top padding `py-10` (40px) on `/employers/[slug]` — already in production, kept for parity (40px is a multiple of 4 ✓).
- Header height `h-16` (64px) and content offset spacer `h-16` — fixed header convention, do not change.
- Tile sizes `h-12 w-12` (48px), `h-14 w-14` (56px) for logo tiles — both are multiples of 4 (48 = 4×12, 56 = 4×14) ✓.
- Floating "Featured" badge offset: `-top-2` (-8px, multiple of 4). NOTE: The existing `FeaturedJobCard.astro` ships with `-top-2.5` (-10px) — Phase 4 work must update this to `-top-2` for spec compliance (badge nudge adjusted from -10px to -8px; visually indistinguishable, brings spacing back onto the 4-pt grid).

Touch-target minimum: **40px** (`h-10` Button-lg) for primary actions on mobile; **44px** equivalent achieved by `h-9` (36px) buttons because the underlying tap region in Astro `<a>` / `<button>` extends to the parent card via `block group` pattern. Standalone icon buttons in dashboard MUST use `h-10 w-10` (40px) minimum.

---

## Typography

Single family (Geist Variable). **Exactly 4 sizes, exactly 2 weights** (regular 400 + semibold 600). Body line-height 1.5, heading 1.2 (Tailwind `leading-tight` ≈ 1.25 is acceptable).

| Role | Size | Tailwind | Weight | Line Height | Usage |
|------|------|----------|--------|-------------|-------|
| Body | 14px | `text-sm` | 400 | 1.5 (`leading-relaxed`) | Default text inside cards, form helpers, table rows, meta lines (location/posted-ago/salary), uppercase eyebrows (with `text-[10px]`-equivalent let-down via `tracking-wider` is NOT used — eyebrows stay at 14px semibold), badge text, mobile-nav labels |
| Body emphasis | 14px | `text-sm` | 600 (`font-semibold`) | 1.5 | Stat values inline, table header cells, eyebrow labels (uppercase + `tracking-wider`), badge text, mobile-nav labels under icons |
| Section heading | 16px | `text-base` | 600 (`font-semibold`) | 1.25 (`leading-tight`) | Card titles, section sub-headings, dashboard tile titles, `JobCardModern` titles |
| Page heading | 20px | `text-xl` | 600 (`font-semibold`) | 1.25 (`leading-tight`) | Section `h2` on dashboard ("Your jobs", "Company profile", "Last 30 days") |
| Display | 24px | `text-2xl` | 600 (`font-semibold`) | 1.2 (`leading-tight`) | Page H1 (`/employers/[slug]`, `/login`, `/auth/check-email`, `/dashboard`, SEO landing pages), dashboard headline stat values (`StatTile` value, `SubscriberMatchCard` count) |

**Strict count: 4 distinct size tokens** — `text-sm` (14px), `text-base` (16px), `text-xl` (20px), `text-2xl` (24px). No `text-xs` and no `text-3xl` in Phase 4. The existing `/jobs` page H1 currently uses `text-3xl font-bold`; Phase 4 work must update that to `text-2xl font-semibold` when touching the page, OR leave untouched if outside Phase 4 scope (do not introduce `text-3xl` in any NEW surface).

**Strict count: 2 weights** — 400 (regular) and 600 (semibold). No 700 (`font-bold`) in any new component. Existing `font-bold` usages in legacy files are out of scope unless modified during this phase, in which case they must be updated to `font-semibold`.

Numbers in tables and stats: `tabular-nums` (already used in `/employers/[slug]` for "{total} open position(s)") — required for all dashboard analytics numbers so columns align across rows.

---

## Color

Light mode tokens (oklch, from `apps/web/src/styles/global.css`). Dark mode mirrors via `.dark` class. Niche accent is injected at the `<html>` level and overrides `--accent`, `--ring`, `--primary`.

| Role | Token | Light value | Dark value | Usage |
|------|-------|-------------|------------|-------|
| Dominant (60%) | `--background` / `bg-background` | `oklch(0.99 0 0)` | `oklch(0.08 0 0)` | Page background, body fill |
| Surface | `--card` / `bg-card` | `oklch(1 0 0)` | `oklch(0.12 0 0)` | Card surfaces (employer header, dashboard tiles, job cards) |
| Secondary (30%) | `--secondary` / `bg-secondary` | `oklch(0.96 0 0)` | `oklch(0.18 0 0)` | Logo initials tile, tag chip background, sidebar fill, "Featured (locked)" pill background |
| Muted text | `--muted-foreground` / `text-muted-foreground` | `oklch(0.45 0 0)` | `oklch(0.65 0 0)` | Meta text, helper text, empty-state body copy |
| Accent (10%) | `--accent` / `bg-accent` / `text-accent` | injected from `niche.branding.accentColor` (wind-turbine: `#2ecc71`) | same — niche cascade applies in dark mode too | RESERVED — see list below |
| Primary | `--primary` / `bg-primary` | injected from `niche.branding.primaryColor` (wind-turbine: `#1a6b3c`) | same | Primary CTA fill (`Post a Job`, `Send magic link`, `Save changes`), submit buttons |
| Destructive | `--destructive` / `bg-destructive` | `oklch(0.577 0.245 27.325)` (red) | same | Destructive actions only: `Unfeature job` confirmation, profile reset, account sign-out (text-only) |
| Border | `--border` / `border-border` | `oklch(0.92 0 0)` | `oklch(0.22 0 0)` | All borders; lower-opacity variants `border-border/50` for backdrop, `border-accent/30` for featured-card edge |
| Input | `--input` / `border-input` | `oklch(0.96 0 0)` | `oklch(0.18 0 0)` | Form field borders |
| Ring | `--ring` / `ring-ring` | follows `--accent` | follows `--accent` | Focus ring (2px, offset-2) — accessibility |

**Accent reserved for** (explicit allow-list — Phase 4 may NOT introduce accent on any other element):
1. Floating **Featured** badge background on `FeaturedJobCard` and pinned job cards (`bg-accent text-accent-foreground`)
2. Hover-state title color on job cards (`group-hover:text-accent`)
3. Inline link text on employer page ("Visit careers page →", "Clear all", "Edit profile" link to locked modal) — `text-accent hover:underline`
4. Brand mark tile in `Header` (`bg-accent text-accent-foreground`) — already shipped
5. Focus ring on all inputs/buttons (`ring-ring`)
6. **Featured ON** toggle thumb fill (dashboard toggle in active state) — `bg-accent`
7. Subscriber-count headline number on dashboard "New matching candidates (this week)" card — `text-accent` on the count integer only, label stays foreground
8. SEO landing page intro keyword highlight — `text-accent` on the niche name in the intro paragraph (single word only)
9. "Claim this listing" CTA outline button hover state (`hover:bg-accent hover:text-accent-foreground` — matches existing `Button variant="outline"` behavior)

**Primary** is the default CTA fill — Claim form submit, magic-link submit, dashboard "Save profile" (locked button shows muted state). Primary and Accent are visually distinct in wind-turbine niche (`#1a6b3c` deep green vs `#2ecc71` bright green) but in some other niches will be similar — this is acceptable.

**60/30/10 verification (wind-turbine niche):**
- 60% `bg-background` (page) + `bg-card` (surfaces) — neutral whites/dark-greys
- 30% `bg-secondary` (logo tiles, sidebar, tag chips, locked-state fills) + `border-border` strokes — light/dark neutrals
- 10% `bg-accent` + `text-accent` (locations listed above only) + `bg-primary` (CTAs) — green family

---

## Component Inventory

New components to ship in Phase 4. Each composes the existing `ui/*` primitives.

### Public surfaces

| Component | Path | Composes | Purpose |
|-----------|------|----------|---------|
| `ClaimListingCTA.astro` | `apps/web/src/components/employer/ClaimListingCTA.astro` | `Button` (variant=`outline`, size=`lg`) | Sits in the employer header card, opens claim modal |
| `ClaimListingModal.astro` | `apps/web/src/components/employer/ClaimListingModal.astro` | `Card`, `Input` (type=`email`), `Button` (variant=`default`, primary) | Email input + submit, posts to `/api/employer/claim` |
| `EmployerLogo.astro` | `apps/web/src/components/employer/EmployerLogo.astro` | none (img + fallback) | Renders `https://img.logo.dev/{employer.domain}?token=...` with `onerror` fallback to existing two-letter initials tile (`bg-secondary text-muted-foreground`). Size variants: `sm` (h-10 w-10 = 40px), `md` (h-12 w-12 = 48px — `JobCardModern` parity), `lg` (h-14 w-14 = 56px — `/employers/[slug]` header parity). |
| `SeoIntroBlock.astro` | `apps/web/src/components/landing/SeoIntroBlock.astro` | none (prose) | Auto-generated intro paragraph for SEO landing pages: "**N** open {niche.name} jobs in {label} as of {date}." — accent on the niche name word only |

### Auth surfaces

| Component | Path | Composes | Purpose |
|-----------|------|----------|---------|
| `MagicLinkForm.astro` | `apps/web/src/components/auth/MagicLinkForm.astro` | `Card`, `Input`, `Button`, `Badge` | Renders on `/login`; submits work email |
| `CheckEmailNotice.astro` | `apps/web/src/components/auth/CheckEmailNotice.astro` | `Card` | Renders on `/auth/check-email`; mail-icon SVG + clear next-step copy |

### Dashboard surfaces

| Component | Path | Composes | Purpose |
|-----------|------|----------|---------|
| `DashboardLayout.astro` | `apps/web/src/components/dashboard/DashboardLayout.astro` | `Layout` + 2-column nav-and-main grid | Authenticated wrapper, sets `Astro.locals.session`; redirects to `/login` if missing |
| `DashboardNav.astro` | `apps/web/src/components/dashboard/DashboardNav.astro` | none (nav + svg icons) | Left rail on desktop (`w-56 lg:block`) with icon + text label per item. **Mobile bottom-fixed nav bar: icon + visible label below each icon.** Labels render at `text-sm font-semibold` (14px, weight 600) immediately under the 24×24 SVG, centered, with `gap-1` (4px) between icon and label. Each nav item is a real `<a>` link with the visible label as its accessible name (no separate `aria-label` needed). Items: Overview, Jobs, Profile, Sign out (4 items, evenly distributed via `flex justify-around`). |
| `StatTile.astro` | `apps/web/src/components/dashboard/StatTile.astro` | `Card`, `CardHeader`, `CardContent` | Stat block: label (eyebrow `text-sm font-semibold uppercase tracking-wider text-muted-foreground`), value (display, `text-2xl font-semibold tabular-nums`), delta or sub-label (`text-sm text-muted-foreground`) |
| `JobRow.astro` | `apps/web/src/components/dashboard/JobRow.astro` | `Card`, `Badge` (variant=`accent` if featured), `FeaturedToggle` | Per-job dashboard row: title, location, posted_at, views/clicks/applies (`StatInline`), toggle |
| `FeaturedToggle.astro` | `apps/web/src/components/dashboard/FeaturedToggle.astro` | `Button` (size=`sm`, variant=`outline` off / `default` on) — OR a real switch | Toggles `featured_until = NOW() + 30d` ↔ NULL via `/api/jobs/[id]/featured`. Off state: muted outline. On state: `bg-accent text-accent-foreground` thumb. Has a 2-state aria-pressed attribute and a hidden form fallback so it works without JS. |
| `LockedFeatureCard.astro` | `apps/web/src/components/dashboard/LockedFeatureCard.astro` | `Card`, `Badge` (variant=`secondary` with text "Paid plan"), `Button` (variant=`outline`, `disabled`) | Wraps the locked profile editor and locked logo uploader. Greyed via `opacity-60 pointer-events-none` on inner form + an overlay row with `Badge` + tooltip "Available on paid plan — coming in Phase 5". The form below is still rendered for preview so executors don't ship empty boxes. |
| `ProfileEditorPreview.astro` | `apps/web/src/components/dashboard/ProfileEditorPreview.astro` | `Card`, `Input`, textarea, `Button` (disabled) | The actual edit form inside `LockedFeatureCard`. Fields: company name (readonly), tagline, rich description (`<textarea>` — Phase 5 swaps in a sanitized rich editor), benefits list, website. All inputs `disabled`. |
| `LogoUploadPreview.astro` | `apps/web/src/components/dashboard/LogoUploadPreview.astro` | `Card`, `Input` (type=`file`, disabled), `Button` (disabled) | Drag-area preview, current logo (logo.dev or initials), disabled "Upload new logo" button. |
| `SubscriberMatchCard.astro` | `apps/web/src/components/dashboard/SubscriberMatchCard.astro` | `Card`, `Badge` | Headline number (accent-colored count, `text-2xl font-semibold`) + label "new matching subscribers this week" + sub-label "(across {niche.name})". |

### Featured listing surfaces (wire to real data, no new components)

- `apps/web/src/pages/jobs/index.astro` — REMOVE hardcoded dummy `FeaturedJobCard` props; loop real featured query results when present. Each is a real `FeaturedJobCard` instance.
- `apps/web/src/pages/employers/[slug].astro` — sort featured jobs to top of the employer's open-roles list (uses the same `JobCardModern` — its existing `isFeatured` branch already handles the visual).

---

## Interaction States

Every interactive element must render five visual states. Use existing focus-ring pattern (`focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`) — already in `Button.astro` and `Input.astro`.

| Element | Default | Hover | Active/Pressed | Focus | Disabled |
|---------|---------|-------|----------------|-------|----------|
| Primary button (`Button variant="default"`) | `bg-primary text-primary-foreground` | `bg-primary/90` | `bg-primary/80` (add this) | `ring-2 ring-ring ring-offset-2` | `opacity-50 pointer-events-none` |
| Outline CTA (`Button variant="outline"`) | `border border-border bg-background` | `bg-accent text-accent-foreground` | `bg-accent/90` | `ring-2 ring-ring` | `opacity-50` |
| Destructive button | `bg-destructive text-white` | `bg-destructive/90` | `bg-destructive/80` | `ring-2 ring-destructive` | `opacity-50` |
| Featured toggle OFF | `border border-border bg-background text-muted-foreground` | `border-accent/50 text-foreground` | n/a | `ring-2 ring-ring` | `opacity-50` |
| Featured toggle ON | `bg-accent text-accent-foreground border-transparent` | `bg-accent/90` | n/a | `ring-2 ring-ring` | `opacity-50` |
| Input (focus) | `border-input bg-transparent` | n/a | n/a | `ring-2 ring-ring ring-offset-2 border-ring` | `cursor-not-allowed opacity-50` |
| Job card link | `border-border bg-card/50` | `border-accent/50 bg-card` (existing) | n/a | `ring-2 ring-ring` (NEW — add to `JobCardModern` if missing) | n/a |
| Sign-out link | `text-muted-foreground` | `text-destructive` | n/a | `ring-2 ring-destructive` | n/a |
| Mobile bottom-nav item | `text-muted-foreground` (icon + label) | `text-foreground` | n/a (no pressed visual on nav) | `ring-2 ring-ring rounded-md` | n/a |
| Mobile bottom-nav item (current) | `text-accent` (icon + label both) — aria-current="page" | n/a | n/a | `ring-2 ring-ring rounded-md` | n/a |

**Loading state** (forms during async submit):
- Button label swaps to "Sending..." (claim form), "Saving..." (dashboard), "Featuring..." (toggle); button gets `disabled` + `opacity-60`, no spinner SVG required for v1.

**Validation states** (claim form, future profile form):
- Invalid email: `Input` gets `border-destructive` and inline helper `<p class="mt-1 text-sm text-destructive">Enter a valid work email.</p>`. Helper id is wired via `aria-describedby` on the input.

---

## Page Layouts

### 1. `/employers/[slug]` (extend existing)
- Container: `mx-auto max-w-4xl px-4 py-10 sm:px-6 lg:px-8` (unchanged).
- Header card: extend with **`EmployerLogo size="lg"` on the left** (replace inline initials block) and **`ClaimListingCTA` on the right** (right-align with `ml-auto`). Already used `flex items-center gap-4` — keep, add `flex-wrap` for mobile so the Claim CTA wraps under the name on small screens.
- Below header: existing position count + job list (no change), but featured jobs come first (sorted server-side).
- Empty state: existing copy stays; if employer has 0 jobs AND not yet claimed, add a second line under the empty illustration: `<p class="mt-2 text-sm text-muted-foreground">Are you {employer.name}? <a href="#claim" class="text-accent hover:underline">Claim this page</a> to add open roles.</p>`.

### 2. `/login` (new)
- Centered single-column form: `mx-auto max-w-md px-4 py-16`.
- `Card` with `CardHeader` (H1 `Sign in to your employer account`, sub-line `Magic-link login. Use your work email — we'll match it to your company.`), `CardContent` (`MagicLinkForm`), `CardFooter` (helper link: `Don't have an employer page yet? <a href="/jobs" class="text-accent">Browse jobs</a>` — claim flow is the alternative entry).

### 3. `/auth/check-email` (new)
- Same centered form layout as `/login`.
- `Card` with mail-icon SVG (24×24, stroked, `text-accent`), H1 `Check your email`, body `We sent a sign-in link to {email}. It expires in 60 minutes.`, sub `Wrong email? <a href="/login" class="text-accent hover:underline">Try again</a>`.

### 4. `/dashboard` (new — protected)
- Top: `Header` (reused; sign-out replaces "Post a Job" CTA when session present).
- Layout: `mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8` with `lg:flex lg:gap-8`.
- Left: `DashboardNav` (`hidden w-56 shrink-0 lg:block` + sticky `top-24`). **Mobile: bottom-fixed nav bar** (`fixed bottom-0 left-0 right-0 border-t border-border bg-background/95 backdrop-blur lg:hidden`) with **4 items, each rendering icon (24×24) + visible text label below** (`text-sm font-semibold`, 14px). Items: Overview, Jobs, Profile, Sign out. Each item is a real `<a>` link with `flex flex-col items-center gap-1 py-2 px-3 min-h-14`; current page gets `text-accent` + `aria-current="page"`; non-current items use `text-muted-foreground`. Min height per tap target: 56px (h-14) to cover icon + label + padding while staying on the 4-pt grid.
- Right (`min-w-0 flex-1`): sections separated by `mb-8`. Order:
  1. **Greeting row**: H1 `{employer.name}` + sub `Signed in as {user.email}` (muted).
  2. **Stats row** (`grid gap-4 sm:grid-cols-3`): `StatTile`s — 30-day Views, 30-day Clicks, 30-day Apply clicks. Each tile shows raw integer with `tabular-nums`.
  3. **Subscriber match card** (`SubscriberMatchCard`, single full-width card).
  4. **Featured jobs section**: H2 `Your jobs` + helper `{N} active · toggle Featured to pin a job at the top of the listing (free in Phase 4 — unlimited).` then `JobRow` list with toggles.
  5. **Profile (locked)**: H2 `Company profile`, `LockedFeatureCard` containing `ProfileEditorPreview` + `LogoUploadPreview` side-by-side on `md:grid-cols-2`.

### 5. SEO landing pages `/[niche-keyword]-jobs-[modifier]` (new)
- Reuse `/jobs/index.astro` layout but:
  - Replace search header with `SeoIntroBlock` (centered, `max-w-3xl mx-auto py-12 text-center`): H1 `{Label}` (e.g. "Wind Turbine Jobs in Austin, TX"), accented sub-paragraph: `<N> open <accent>{niche.name.toLowerCase()}</accent> jobs in {label} as of {date}.`
  - Below, render filtered `JobCardModern` list (same component, same spacing as `/jobs`).
  - Below the list, render a small SEO footer block: 2–3 sentence boilerplate sourced from `niche.seoFooter` template (multi-niche field — researcher to define in `NicheConfig` type) — falls back to a neutral paragraph if missing.

---

## Copywriting Contract

All copy is final and must ship verbatim — no placeholders. Substitute `{niche.name}` at render time from `Astro.locals.niche.name` (wind-turbine produces "Wind Turbine Jobs").

### Primary CTAs

| Surface | Element | Copy |
|---------|---------|------|
| `/employers/[slug]` | Claim CTA button | `Claim this listing` |
| Claim modal | Submit button | `Send magic link` |
| Claim modal | Cancel button | `Cancel` |
| `/login` | Submit button | `Send magic link` |
| `/dashboard` | Sign-out link | `Sign out` |
| `JobRow` | Toggle (off state) | `Feature job` |
| `JobRow` | Toggle (on state) | `Featured · 12 days left` (compute from `featured_until`; falls back to `Featured` if <1 day) |
| `LockedFeatureCard` | Disabled save button | `Save profile (paid plan)` |
| `LockedFeatureCard` | Disabled upload button | `Upload logo (paid plan)` |
| Mobile bottom-nav | Item labels | `Overview`, `Jobs`, `Profile`, `Sign out` |

### Headings

| Surface | Copy |
|---------|------|
| Claim modal H1 | `Claim {employer.name}` |
| Claim modal body | `Enter your work email at {employer.name}. We'll send you a magic link to sign in and manage this listing.` |
| `/login` H1 | `Sign in to your employer account` |
| `/login` body | `Magic-link login — no password. Use your work email and we'll match it to your company.` |
| `/auth/check-email` H1 | `Check your email` |
| `/auth/check-email` body | `We sent a sign-in link to <strong>{email}</strong>. It expires in 60 minutes. Open it on the same device for the smoothest experience.` |
| Dashboard greeting H1 | `{employer.name}` |
| Dashboard stats section heading | `Last 30 days` (eyebrow) |
| Dashboard jobs section H2 | `Your jobs` |
| Dashboard jobs helper | `Toggle Featured to pin a job to the top of the public listing. Featured slots are unlimited during the free trial — paid tier limits apply from Phase 5.` |
| Subscriber match card label | `New matching subscribers (last 7 days)` |
| Subscriber match card sub | `Across {niche.name}. Resets every Monday 00:00 UTC.` |
| Locked profile editor H2 | `Company profile` |
| Locked profile editor banner | `Profile editing and logo upload unlock with any paid plan (Phase 5). Preview the form below.` |
| SEO landing intro template | `{N} open {niche.name} in {label} as of {date}. Browse positions from {employerCount} employers — updated hourly.` |

### Empty states

| Surface | Heading | Body |
|---------|---------|------|
| Dashboard, 0 jobs | `No active jobs` | `We're not seeing any open positions for {employer.name} right now. New roles appear here automatically when we detect them on your careers page.` |
| Dashboard, 0 views (analytics) | (no separate heading — show `0` in the StatTile) | StatTile sub-label: `No views yet — share your page` (a small `<a class="text-accent">Copy public link</a>` next to it copies `https://{niche.domain}/employers/{slug}` to clipboard) |
| Dashboard, 0 matching subscribers | (StatTile shows `0`) | Sub-label: `No new subscribers this week — total audience is {N}.` |
| `/employers/[slug]`, no jobs | (existing) | (existing) plus the new claim-prompt line under it (see Page Layouts §1) |
| SEO landing, 0 matching jobs | `No matching jobs right now` | `Subscribe to weekly alerts and we'll email you the moment a {niche.name.toLowerCase().replace(/ jobs$/, '')} role appears in {label}.` followed by the existing `Newsletter.astro` form |

### Error states

| Surface | Copy |
|---------|------|
| Claim modal: email domain mismatch | `That email doesn't match the domain we have on file for {employer.name}. If you work there, email us at hello@{niche.domain} and we'll verify manually.` (rendered inline below the input, `text-destructive`) |
| Claim modal: invalid email format | `Enter a valid work email.` |
| Claim modal: rate-limited | `Too many attempts. Try again in a few minutes.` |
| `/login`: send-link failed | `We couldn't send the magic link. Check the email address and try again.` |
| `/auth/callback`: invalid token | H1 `Sign-in link expired` + body `Magic links expire after 60 minutes. <a class="text-accent" href="/login">Request a new one</a>.` |
| Featured toggle: write failed | Inline toast/pill above the JobRow: `Couldn't update Featured status. Try again.` (`bg-destructive/10 text-destructive border border-destructive/30 rounded-md p-2 text-sm`) |
| Dashboard: stats API failed | Each `StatTile` value renders `—` and sub-label says `Stats temporarily unavailable.` |

### Destructive confirmations

Phase 4 has **two** destructive interactions:

| Action | Confirmation pattern | Copy |
|--------|----------------------|------|
| Toggle Featured OFF (a currently-featured job) | Native `confirm()` dialog OR an inline 2-step (preferred for v1: inline button transforms to `Remove featured? <a>Yes</a> · <a>Cancel</a>`) | `Remove featured status from "{job.title}"? It will drop out of the pinned section immediately.` |
| Sign out | None — direct action (low-risk, reversible by signing back in) | `Sign out` button does it directly |

Profile editing in Phase 4 is locked — no save action, no destructive action there.

---

## Accessibility Contract

| Requirement | Implementation |
|-------------|----------------|
| Contrast | All text against its background must hit WCAG AA (4.5:1 for body, 3:1 for ≥18px/600). Niche accent `#2ecc71` on white fails AA for body — it is therefore RESERVED for non-body usage only (badges, focus rings, headline integers ≥24px, hover indicators, single-word emphasis). Body links use `text-accent` only on hover where they also gain underline — never as the resting color on body-size text. |
| Focus visibility | Every interactive element has `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`. No `outline-none` without a `focus-visible` replacement. |
| Touch targets | Minimum 40×40 px (`h-10 w-10`) for icon-only buttons. Text buttons may be 36 px (`h-9`) when min-width ≥ 80 px (Fitts compliance via horizontal extent). Mobile bottom-nav items use icon + visible label, so the visible label IS the accessible name (no `aria-label` needed). Each nav item has `min-h-14` (56px) tap region. |
| Keyboard | Featured toggle: `<button>` with `aria-pressed`. Dashboard nav: real `<a>` links. Claim modal: trap focus inside the modal, ESC closes, returns focus to the trigger. |
| Screen readers | Logo-tile fallback initials get `aria-label="{employer.name} logo"`. Stat values get `aria-label="{value} {metric} in the last 30 days"`. Toggle's accessible name is the verb only — `aria-label="Feature {job.title}"` or `aria-label="Remove featured status from {job.title}"`. Mobile-nav items have visible text labels, so no extra `aria-label` is required; the current page item adds `aria-current="page"`. |
| Color independence | Featured pinning is conveyed by BOTH the "Featured" badge text AND the accent border — never accent border alone. Mobile-nav current state uses BOTH accent color AND `aria-current="page"` — never color alone. |
| Form labels | Every `Input` has a real `<label>` (visible or `sr-only`) — no placeholder-only fields. |
| Motion | Existing `transition-all` / `transition-colors` is fine (subtle); honor `prefers-reduced-motion: reduce` — add to global.css if not present: `@media (prefers-reduced-motion: reduce) { *, ::before, ::after { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; } }`. |

---

## Responsive Breakpoints

Tailwind defaults; no custom breakpoints.

| Width | Behavior |
|-------|----------|
| `< 640px` (mobile) | Single column. Dashboard nav becomes bottom bar (icon + label per item). Claim CTA wraps under employer name on `/employers/[slug]`. Stats grid stacks (`grid-cols-1`). Profile editor stacks. |
| `640px–1023px` (sm/md) | Stats `grid sm:grid-cols-3`. Profile editor `md:grid-cols-2`. Dashboard nav still bottom-bar (icon + label) until `lg`. |
| `≥ 1024px` (lg+) | Dashboard nav becomes left rail (`w-56 sticky top-24`, icon + label inline horizontally per item). `/jobs` sidebar visible. Max content width caps at `max-w-7xl` (1280px). |

---

## Logo & Asset Sources

| Asset | Source | Fallback |
|-------|--------|----------|
| Employer logo (auto) | `https://img.logo.dev/{employer.domain}?token={LOGODEV_TOKEN}&size=120&format=png` — token kept in Pages secret `LOGODEV_TOKEN`. Requires `employer.domain` (added in this phase per D-02). | Two-letter initials tile (`bg-secondary text-muted-foreground rounded-lg` — existing pattern) |
| Employer logo (paid override, Phase 5) | Supabase Storage signed URL, key = `employers/{employer.id}/logo.{ext}` | logo.dev → initials chain |
| OG image | `https://{niche.domain}/og-default.png` (existing) | n/a |
| Favicons | `icon.svg` (existing) | n/a |

`EmployerLogo.astro` MUST render both an `<img>` and a sibling fallback `<div>`, with `onerror` on the img swapping to the fallback (no JS-blocking layout shift). Use `width="48" height="48"` (or sized variant) to reserve space.

---

## Multi-Niche Compliance Checklist

The hard rule from `CLAUDE.md` is **never hardcode `wind_turbine` schema or "Wind Turbine" copy**. Every UI surface in Phase 4:

- [ ] Reads niche name from `Astro.locals.niche.name` — never literal "Wind Turbine Jobs"
- [ ] Reads niche slug from `Astro.locals.niche.slug` for landing-page URL prefix
- [ ] Reads niche domain from `Astro.locals.niche.domain` for `mailto:` and clipboard share
- [ ] Reads accent/primary colors from `niche.branding.*` — `--accent` / `--primary` CSS vars only, never hex
- [ ] SEO landing pages list comes from `niche.landingPages[]` config (new field — researcher/planner adds to `NicheConfig` type)
- [ ] Empty-state copy and intro template use `{niche.name}` substitution at runtime
- [ ] Subscriber match card scopes to `niche.supabaseSchema` server-side (not a UI rule but called out for the planner)

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none (project does not use the shadcn CLI; design tokens are hand-rolled in the same shape) | not required |
| third-party | none | not applicable |

No third-party UI registry packages are introduced in Phase 4. All new components live under `apps/web/src/components/` and compose existing primitives.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS — all CTAs, headings, empty states, errors, and confirmations specified verbatim; mobile-nav labels declared
- [ ] Dimension 2 Visuals: PASS — every new component listed with composition path; reuses existing primitives; mobile bottom-nav declared as icon + visible label
- [ ] Dimension 3 Color: PASS — accent has explicit 9-item allow-list, 60/30/10 verified
- [ ] Dimension 4 Typography: PASS — exactly 4 sizes (`text-sm`, `text-base`, `text-xl`, `text-2xl`), exactly 2 weights (400, 600); no `text-xs`, no `text-3xl`, no `font-bold` in new components
- [ ] Dimension 5 Spacing: PASS — multiples of 4 only; floating badge offset adjusted from `-top-2.5` (-10px) to `-top-2` (-8px) for grid compliance
- [ ] Dimension 6 Registry Safety: PASS — no third-party registry; not applicable

**Approval:** pending
