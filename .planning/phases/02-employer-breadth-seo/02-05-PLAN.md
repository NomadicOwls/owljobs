---
phase: 02-employer-breadth-seo
plan: 05
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - packages/ats-adapters/src/smartrecruiters.ts
  - packages/ats-adapters/package.json
  - packages/ats-adapters/test/smartrecruiters.test.ts
  - workers/ingest/src/ingest.ts
  - niches/wind-turbine.ts
autonomous: true
requirements: [COVG-02]
must_haves:
  truths:
    - "fetchAllSmartRecruitersJobs(target) returns adapted jobs (title, location, canonicalUrl, sourceId) when SmartRecruiters API responds OK"
    - "ingestSmartRecruiters function in ingest.ts replaces the Plan 04 stub branch with real upsertJob + creation pings + expireMissingJobs"
    - "upsertEmployer for SmartRecruiters stores companyId in ats_site (so fetch-description.ts can read it)"
    - "At least 2 SmartRecruiters wind employers added to niches/wind-turbine.ts"
    - "smartrecruiters.test.ts asserts the adapter contract"
  artifacts:
    - path: "packages/ats-adapters/src/smartrecruiters.ts"
      provides: "SmartRecruiters list-endpoint adapter (lazy-fetch)"
      min_lines: 80
      exports: ["fetchAllSmartRecruitersJobs", "SmartRecruitersAdapterError"]
    - path: "packages/ats-adapters/test/smartrecruiters.test.ts"
      provides: "unit tests for response normalization"
      min_lines: 40
    - path: "niches/wind-turbine.ts"
      provides: "SmartRecruiters atsTargets entries"
      contains: "atsType: \"smartrecruiters\""
  key_links:
    - from: "packages/ats-adapters/src/smartrecruiters.ts"
      to: "SmartRecruiters Postings API"
      via: "fetch GET /v1/companies/{id}/postings?status=PUBLIC"
      pattern: "api\\.smartrecruiters\\.com"
    - from: "workers/ingest/src/ingest.ts"
      to: "ingestSmartRecruiters function"
      via: "real branch replacing Plan 04 stub"
      pattern: "ingestSmartRecruiters"
---

## Phase Goal

**As a** wind turbine technician, **I want to** see openings from SmartRecruiters-using wind employers (e.g. discovered via Plan 08), **so that** I cover companies my Workday/SuccessFactors search misses.

<objective>
Build the SmartRecruiters adapter — D-21 priority 2 in the Wave 1 activation order. SmartRecruiters has a public REST API (no auth required) and is "high leverage" per D-09: each new SmartRecruiters employer added to the niche config goes from 0 to N jobs ingested with no per-employer code.

Architecture is LAZY-FETCH (matches Workday pattern):
- List endpoint `GET /v1/companies/{companyId}/postings?status=PUBLIC&limit=100&offset=N` returns titles + locations + posting IDs (NO descriptions)
- Detail endpoint (already wired in Plan 04 fetch-description.ts) fills descriptions during the enrich queue stage
- Pitfall 3 enforced: list adapter sets `description: null` always

Plan 04 added the `SmartRecruitersTarget` interface and a STUB branch in `ingestNiche`. This plan replaces the stub with the real `ingestSmartRecruiters` function and registers at least 2 wind-turbine employers.

Purpose: COVG-02 — adds breadth toward the 20-employer gate. Specific employers selected via discovery (Plan 08) or known wind SmartRecruiters tenants (search "wind" on jobs.smartrecruiters.com).

Output:
- `packages/ats-adapters/src/smartrecruiters.ts` — list adapter + error class
- `packages/ats-adapters/package.json` — add `./smartrecruiters` export entry (matches existing pattern)
- `packages/ats-adapters/test/smartrecruiters.test.ts` — unit tests on response normalization (Wave 0 file — directory does not exist yet, must mkdir)
- `workers/ingest/src/ingest.ts` — real `ingestSmartRecruiters` function replacing Plan 04 stub
- `niches/wind-turbine.ts` — at least 2 SmartRecruiters atsTargets (e.g. discovered companies; commit at least one verified-live tenant)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-employer-breadth-seo/02-CONTEXT.md
@.planning/phases/02-employer-breadth-seo/02-RESEARCH.md
@packages/ats-adapters/src/workday.ts
@packages/ats-adapters/src/recruitee.ts
@packages/ats-adapters/src/sanitize.ts
@packages/ats-adapters/package.json
@workers/ingest/src/ingest.ts
@workers/ingest/src/fetch-description.ts
@niches/wind-turbine.ts
@.planning/phases/02-employer-breadth-seo/02-04-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — smartrecruiters.test.ts (test directory + adapter contract)</name>
  <files>packages/ats-adapters/test/smartrecruiters.test.ts</files>
  <read_first>
    - workers/ingest/test/upsert.test.ts (source-contract pattern)
    - packages/ats-adapters/src/recruitee.ts (peer adapter — sourceUrl/canonicalUrl/sourceId pattern)
    - .planning/phases/02-employer-breadth-seo/02-RESEARCH.md (lines 290-345 — list endpoint shape)
  </read_first>
  <behavior>
    - Test 1: smartrecruiters.ts source contains a function `fetchAllSmartRecruitersJobs`
    - Test 2: source URL hits api.smartrecruiters.com/v1/companies/{companyId}/postings
    - Test 3: status=PUBLIC query param is set
    - Test 4: pagination uses offset + limit (response shape from research)
    - Test 5: adapter returns AdaptedJob with description: null (lazy fetch — Pitfall 3)
    - Test 6: SmartRecruitersAdapterError class is exported (consistent with Recruitee/SF/Softgarden pattern)
  </behavior>
  <action>
    The directory `packages/ats-adapters/test/` does not exist yet. Create it and add the test file.

    From repo root:

        mkdir -p packages/ats-adapters/test

    Create `packages/ats-adapters/test/smartrecruiters.test.ts`:

        import { describe, it, expect, beforeAll } from "vitest";
        import { readFile } from "node:fs/promises";

        describe("smartrecruiters.ts — adapter contract (COVG-02, D-09, Pitfall 3)", () => {
          let src = "";
          beforeAll(async () => {
            src = await readFile("packages/ats-adapters/src/smartrecruiters.ts", "utf-8");
          });

          it("exports fetchAllSmartRecruitersJobs", () => {
            expect(src).toMatch(/export\s+(async\s+)?function\s+fetchAllSmartRecruitersJobs/);
          });

          it("exports SmartRecruitersAdapterError class", () => {
            expect(src).toMatch(/export\s+class\s+SmartRecruitersAdapterError/);
          });

          it("calls the public Postings list endpoint with status=PUBLIC", () => {
            expect(src).toMatch(/api\.smartrecruiters\.com\/v1\/companies\/[\$\{][^}]+\}\/postings/);
            expect(src).toMatch(/status=PUBLIC/);
          });

          it("paginates via offset + limit", () => {
            expect(src).toMatch(/offset/);
            expect(src).toMatch(/limit/);
          });

          it("sets description: null for all returned jobs (Pitfall 3 — lazy fetch)", () => {
            expect(src).toMatch(/description:\s*null/);
          });

          it("returns AdaptedJob shape (title, canonicalUrl, sourceId, etc.)", () => {
            expect(src).toMatch(/sourceId/);
            expect(src).toMatch(/canonicalUrl/);
            expect(src).toMatch(/title/);
          });

          it("imports sha256Hex from @owljobs/schema for sourceId", () => {
            expect(src).toMatch(/import\s*\{[^}]*sha256Hex[^}]*\}\s*from\s*["']@owljobs\/schema["']/);
          });

          it("does NOT include detail-endpoint fetch in list adapter (lazy)", () => {
            // Detail endpoint is in fetch-description.ts (Plan 04) — list must not duplicate
            // Allow the URL prefix but disallow `/postings/{` followed by an `id` interpolation
            expect(src).not.toMatch(/api\.smartrecruiters\.com\/v1\/companies\/[\$\{][^}]+\}\/postings\/[\$\{]/);
          });
        });

    These tests fail RED at this task — Task 2 implements the adapter to make them pass.
  </action>
  <verify>
    <automated>test -d packages/ats-adapters/test && test -f packages/ats-adapters/test/smartrecruiters.test.ts && pnpm vitest run packages/ats-adapters/test/smartrecruiters.test.ts --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Directory exists: `test -d packages/ats-adapters/test`
    - File exists: `test -f packages/ats-adapters/test/smartrecruiters.test.ts`
    - Tests fail RED at this point (expected): `pnpm vitest run packages/ats-adapters/test/smartrecruiters.test.ts` exits NON-zero (file not found error or assertion failures)
    - Test file uses source-contract pattern (no live HTTP): `grep -q "from .node:fs/promises." packages/ats-adapters/test/smartrecruiters.test.ts`
  </acceptance_criteria>
  <done>Test directory + smartrecruiters.test.ts created; tests fail RED (expected — adapter not yet built).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SmartRecruiters list adapter</name>
  <files>packages/ats-adapters/src/smartrecruiters.ts, packages/ats-adapters/package.json</files>
  <read_first>
    - packages/ats-adapters/src/recruitee.ts (peer adapter — error class + AdaptedJob pattern)
    - packages/ats-adapters/src/workday.ts (reference for sourceId via sha256Hex of canonical URL)
    - packages/ats-adapters/src/sanitize.ts (sanitizeJobDescription — list does NOT use it; descriptions are null)
    - packages/ats-adapters/package.json (existing exports — must add `./smartrecruiters`)
    - .planning/phases/02-employer-breadth-seo/02-RESEARCH.md (lines 290-345 — exact list endpoint response shape)
    - packages/niches/src/index.ts (just-extended SmartRecruitersTarget interface from Plan 04)
  </read_first>
  <behavior>
    - Behavior 1: For a target with companyId="Vestas", calls https://api.smartrecruiters.com/v1/companies/Vestas/postings?status=PUBLIC&limit=100&offset=0
    - Behavior 2: Reads `content[]` array from response, for each posting builds AdaptedJob with title, location (from posting.location.city + region + country), canonicalUrl=`https://jobs.smartrecruiters.com/{companyId}/{posting.id}`, sourceId=sha256(canonicalUrl), description=null
    - Behavior 3: If totalFound > limit, paginates via offset until all rows fetched (max 1000 to avoid runaway)
    - Behavior 4: HTTP error throws SmartRecruitersAdapterError with statusCode
    - Behavior 5: Empty `content[]` returns []
  </behavior>
  <action>
    Create `packages/ats-adapters/src/smartrecruiters.ts`:

        import { sha256Hex } from "@owljobs/schema";
        import type { SmartRecruitersTarget } from "@owljobs/niches";

        // SmartRecruiters Postings API list response shape (verified — Phase 2 RESEARCH.md lines 290-345).
        interface SmartRecruitersListPosting {
          id: string;
          uuid?: string;
          name: string;
          releasedDate?: string;
          location?: {
            country?: string;
            region?: string;
            city?: string;
            remote?: boolean;
          };
          ref?: string;
        }

        interface SmartRecruitersListResponse {
          limit: number;
          offset: number;
          totalFound: number;
          content: SmartRecruitersListPosting[];
        }

        export interface AdaptedJob {
          title: string;
          location: string;
          postedOn: string | null;
          canonicalUrl: string;
          sourceUrl: string;
          employerName: string;
          sourceId: string;
          rawPayload: string;
          description: string | null;   // ALWAYS null for SmartRecruiters list (Pitfall 3 — lazy fetch in enrich)
        }

        const PAGE_SIZE = 100;
        const MAX_RECORDS = 1000;

        export class SmartRecruitersAdapterError extends Error {
          constructor(
            message: string,
            public readonly statusCode: number,
          ) {
            super(message);
            this.name = "SmartRecruitersAdapterError";
          }
        }

        function buildLocation(loc?: SmartRecruitersListPosting["location"]): string {
          if (!loc) return "";
          if (loc.remote) return "Remote";
          return [loc.city, loc.region, loc.country].filter(Boolean).join(", ");
        }

        async function fetchPage(
          companyId: string,
          offset: number,
          limit: number,
        ): Promise<SmartRecruitersListResponse> {
          const url = `https://api.smartrecruiters.com/v1/companies/${encodeURIComponent(companyId)}/postings?status=PUBLIC&limit=${limit}&offset=${offset}`;
          const resp = await fetch(url, { headers: { Accept: "application/json" } });
          if (!resp.ok) {
            throw new SmartRecruitersAdapterError(
              `SmartRecruiters list ${resp.status} for ${companyId} (offset=${offset})`,
              resp.status,
            );
          }
          return (await resp.json()) as SmartRecruitersListResponse;
        }

        export async function fetchAllSmartRecruitersJobs(
          target: SmartRecruitersTarget,
        ): Promise<AdaptedJob[]> {
          const out: AdaptedJob[] = [];
          let offset = 0;

          while (out.length < MAX_RECORDS) {
            const page = await fetchPage(target.companyId, offset, PAGE_SIZE);
            for (const p of page.content) {
              const canonicalUrl = `https://jobs.smartrecruiters.com/${target.companyId}/${p.id}`;
              const sourceId = await sha256Hex(canonicalUrl);
              out.push({
                title: p.name,
                location: buildLocation(p.location),
                postedOn: p.releasedDate ?? null,
                canonicalUrl,
                sourceUrl: canonicalUrl,
                employerName: target.employer,
                sourceId,
                rawPayload: JSON.stringify(p),
                description: null,
              });
            }
            offset += PAGE_SIZE;
            if (offset >= page.totalFound) break;
            if (page.content.length === 0) break;
          }

          return out;
        }

    Then add the export entry to `packages/ats-adapters/package.json`. Insert after the `./softgarden` entry in the `exports` object:

        "./smartrecruiters": {
          "types": "./src/smartrecruiters.ts",
          "default": "./src/smartrecruiters.ts"
        }

    Constraints:
    - Do NOT call sanitizeJobDescription (list has no descriptions).
    - Do NOT fetch detail endpoint here — that's fetch-description.ts.
    - `description: null` ALWAYS — Pitfall 3.
    - Use `encodeURIComponent` on companyId for safety.
    - MAX_RECORDS guard prevents runaway pagination.
  </action>
  <verify>
    <automated>pnpm vitest run packages/ats-adapters/test/smartrecruiters.test.ts --reporter=verbose && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All 8 tests in `smartrecruiters.test.ts` pass: `pnpm vitest run packages/ats-adapters/test/smartrecruiters.test.ts` exits 0
    - `pnpm tsc --noEmit` passes
    - File exists with adapter exports: `grep -q "export async function fetchAllSmartRecruitersJobs" packages/ats-adapters/src/smartrecruiters.ts && grep -q "export class SmartRecruitersAdapterError" packages/ats-adapters/src/smartrecruiters.ts`
    - package.json export entry present: `grep -q "./smartrecruiters" packages/ats-adapters/package.json`
    - description is always null: `grep -q "description: null" packages/ats-adapters/src/smartrecruiters.ts`
  </acceptance_criteria>
  <done>Adapter built, exports registered, all tests pass, tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace stub branch with real ingestSmartRecruiters in ingest.ts</name>
  <files>workers/ingest/src/ingest.ts</files>
  <read_first>
    - workers/ingest/src/ingest.ts (current state — Plan 03 added pings + buildPublicUrl, Plan 04 added smartrecruiters STUB branch)
    - packages/ats-adapters/src/smartrecruiters.ts (just-built adapter)
    - workers/ingest/src/ingest.ts ingestRecruitee function (lines 241-294 — closest pattern to follow: lazy adapter, no eager description, expireMissingJobs at end)
  </read_first>
  <behavior>
    - Behavior 1: For each SmartRecruitersTarget, fetches jobs via fetchAllSmartRecruitersJobs
    - Behavior 2: Calls upsertEmployer with atsType="smartrecruiters" and ats_site=companyId (so fetch-description.ts can read it)
    - Behavior 3: For each job, calls upsertJob with source="smartrecruiters", postedAt=postedOn, description=null
    - Behavior 4: Creation pings fire after each successful insert (using buildPublicUrl per Plan 03)
    - Behavior 5: expireMissingJobs called at end (SmartRecruiters returns full company snapshot — safe to expire absent jobs, unlike aggregators)
    - Behavior 6: Errors do not bubble up; logged + counted in stats.errors
  </behavior>
  <action>
    Modify `workers/ingest/src/ingest.ts`. First, also need to update `JobInput.source` type union to include `"smartrecruiters"` (Plan 03 added adzuna/jsearch but smartrecruiters was deferred to here):

    Step A — Update the `JobInput.source` union (line 423). Add `"smartrecruiters"` and `"trakstar"` (Plan 07 will need it; cheaper to add both now):

    From:

        source: "workday" | "greenhouse" | "successfactors" | "recruitee" | "softgarden" | "adzuna" | "jsearch";

    To:

        source: "workday" | "greenhouse" | "successfactors" | "recruitee" | "softgarden" | "smartrecruiters" | "trakstar" | "adzuna" | "jsearch";

    Step B — Add the import for the SmartRecruiters adapter at the top of ingest.ts (alongside other adapter imports):

        import { fetchAllSmartRecruitersJobs, SmartRecruitersAdapterError } from "@owljobs/ats-adapters/smartrecruiters";
        import type { SmartRecruitersTarget } from "@owljobs/niches";

    Step C — Add the `ingestSmartRecruiters` function. Insert AFTER the existing `ingestSoftgarden` function (around line 350) and BEFORE the `parseWorkdayDate` helper. Follow the ingestRecruitee pattern (lines 241-294):

        async function ingestSmartRecruiters(
          target: SmartRecruitersTarget,
          niche: NicheConfig,
          db: SchemaClient,
          stats: IngestStats,
          saJson?: string,
          budget?: { remaining: number },
        ): Promise<void> {
          let jobs;
          try {
            jobs = await fetchAllSmartRecruitersJobs(target);
          } catch (err) {
            if (err instanceof SmartRecruitersAdapterError) {
              console.warn(`[ingest] SmartRecruiters ${err.statusCode} for ${target.employer} — verify companyId`);
            }
            throw err;
          }

          // ats_site stores companyId — fetch-description.ts reads it (Plan 04)
          const employerId = await upsertEmployer(db, {
            name: target.employer,
            atsType: "smartrecruiters",
            atsSite: target.companyId,
            careersUrl: `https://jobs.smartrecruiters.com/${target.companyId}`,
          });

          const fetchedJobIds = new Set<string>();
          for (const job of jobs) {
            fetchedJobIds.add(job.sourceId);
            try {
              const inserted = await upsertJob(db, {
                id: job.sourceId,
                title: job.title,
                employerId,
                location: job.location,
                canonicalUrl: job.canonicalUrl,
                postedAt: job.postedOn,        // already ISO from SmartRecruiters
                source: "smartrecruiters",
                rawPayload: JSON.parse(job.rawPayload) as Record<string, unknown>,
              });
              if (inserted) {
                stats.inserted++;
                if (saJson && budget && budget.remaining > 0) {
                  try {
                    const r = await pingUrlUpdated(saJson, buildPublicUrl(niche, job.sourceId));
                    budget.remaining--;
                    if (r.ok) stats.pinged++; else stats.pingFailures++;
                  } catch (err) {
                    console.warn(`[indexing] creation ping failed for ${job.sourceId}:`, err);
                    stats.pingFailures++;
                  }
                }
              } else {
                stats.skipped++;
              }
            } catch (err) {
              console.error(`[ingest] failed to upsert job "${job.title}":`, err);
              stats.errors++;
            }
          }

          try {
            const r = await expireMissingJobs(db, employerId, fetchedJobIds, saJson);
            stats.expired += r.marked;
            stats.pinged += r.pinged;
            stats.pingFailures += r.pingFailures;
          } catch (err) {
            console.error(`[expire] ${target.employer}:`, err);
            stats.errors++;
          }
        }

    Step D — Replace the stub branch in `ingestNiche`. Find the block:

        } else if (target.atsType === "smartrecruiters") {
          // STUB — real implementation in Plan 05
          console.log(`[ingest] smartrecruiters adapter not yet implemented (Plan 05): ${target.employer}`);
        }

    Replace with:

        } else if (target.atsType === "smartrecruiters") {
          await ingestSmartRecruiters(target, niche, db, localStats, saJson, budget);
        }

    Constraints:
    - Do NOT touch the trakstar / adzuna / jsearch stubs — those are for Plans 06/07.
    - Reuse the budget object from ingestNiche (per-target budget — already established by Plan 03).
    - `expireMissingJobs` IS called for SmartRecruiters (employer-complete snapshot, like Workday). NOT skipped — that's only for aggregators (Plan 06).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5 && pnpm vitest run workers/ingest/test/ --reporter=verbose 2>&1 | tail -10 && grep -q "ingestSmartRecruiters" workers/ingest/src/ingest.ts</automated>
  </verify>
  <acceptance_criteria>
    - `ingestSmartRecruiters` function defined: `grep -q "async function ingestSmartRecruiters" workers/ingest/src/ingest.ts`
    - Stub replaced: `! grep "STUB.*Plan 05" workers/ingest/src/ingest.ts`
    - Branch dispatches to real function: `grep -q "ingestSmartRecruiters(target, niche, db, localStats, saJson, budget)" workers/ingest/src/ingest.ts`
    - JobInput.source union includes "smartrecruiters": `grep -q "smartrecruiters" workers/ingest/src/ingest.ts` (multi-occurrence — function name + source string + branch)
    - `pnpm tsc --noEmit` passes
    - All existing worker tests still pass: `pnpm vitest run workers/ingest/test/` exits 0
    - `expireMissingJobs` IS called inside ingestSmartRecruiters: `grep -A 80 "async function ingestSmartRecruiters" workers/ingest/src/ingest.ts | grep -q "expireMissingJobs"`
    - `ats_site: target.companyId` written to upsertEmployer (so fetch-description.ts can use it): `grep -A 30 "async function ingestSmartRecruiters" workers/ingest/src/ingest.ts | grep -q "atsSite: target.companyId"`
  </acceptance_criteria>
  <done>Real ingestSmartRecruiters wired; stub removed; tests pass; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Add at least 2 SmartRecruiters wind employers to wind-turbine niche</name>
  <files>niches/wind-turbine.ts</files>
  <read_first>
    - niches/wind-turbine.ts (current state — Wave 1 active per Plan 01)
    - packages/niches/src/index.ts (SmartRecruitersTarget shape: employer, atsType, companyId, optional cronGroup)
    - .planning/phases/02-employer-breadth-seo/02-CONTEXT.md (D-09 — SmartRecruiters explicitly called out as discovery target)
    - .planning/phases/02-employer-breadth-seo/02-RESEARCH.md (companyId lookup heuristic — slug from career page URL)
  </read_first>
  <action>
    Add at least 2 SmartRecruiters atsTargets to `niches/wind-turbine.ts` `atsTargets` array.

    Pre-implementation verification step — for each candidate companyId, the implementer MUST verify the API responds 200 OK before committing the entry:

        curl -sI "https://api.smartrecruiters.com/v1/companies/{companyId}/postings?status=PUBLIC&limit=1" | head -1

    Expected: `HTTP/2 200`. If 404, the companyId is wrong (likely uses a different slug). If 401, the company has restricted public listings — skip.

    Candidate wind employers to probe (from market knowledge + Plan 08 results once available):
    - "Vestas" — confirm whether they use SmartRecruiters in addition to SF; Plan 08 discovery may surface this
    - "BoschRexroth" — has a wind division
    - "Goldwind" — career page may be SmartRecruiters-backed (Plan 01 noted "likely custom ATS, skip" — re-verify)
    - Any other from Plan 08 candidates table results (this task may need to defer to a Plan 08 candidates query)

    Pragmatic approach for the executor:
    1. Run the curl probe for 3-5 candidate slugs from the list above + any from a sibling Plan 08 results query (`SELECT name, careers_url FROM public.candidates WHERE ats_type = 'smartrecruiters' AND status = 'detected' ORDER BY confidence DESC LIMIT 10`)
    2. Pick the FIRST 2 that respond 200 with at least 1 job in `content[]`
    3. Add them to `atsTargets` in `niches/wind-turbine.ts` under a new section header `// ── Wave 2: SmartRecruiters (D-09) ──`

    Insert format:

        // ── Wave 2: SmartRecruiters (D-09) ──
        {
          employer: "Verified Employer Name",
          atsType: "smartrecruiters",
          companyId: "VerifiedCompanyId",
        },
        {
          employer: "Second Verified Employer",
          atsType: "smartrecruiters",
          companyId: "SecondVerifiedCompanyId",
        },

    If fewer than 2 SmartRecruiters tenants can be verified live (e.g. all candidates 404), add ONLY the 1 that works and document in the SUMMARY that the second slot remains open pending Plan 08 discovery results. Do NOT add unverified entries — they would only produce ingest errors.

    Constraints:
    - Do NOT add unverified companyIds — Workday adapter pattern (`siteName 422`) handles errors gracefully but populates noisy logs.
    - The `cronGroup` field is optional; default ("hourly") is fine for SmartRecruiters.
    - Do NOT touch existing entries.
  </action>
  <verify>
    <automated>node -e "import('./niches/wind-turbine.ts').then(m => { const t = m.default.atsTargets; const sr = t.filter(x => x.atsType === 'smartrecruiters'); console.log('smartrecruiters count:', sr.length); console.log('targets:', sr.map(x => x.employer + '/' + x.companyId)); if (sr.length < 1) process.exit(1); })" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - At least 1 SmartRecruiters target exists: `grep -c "atsType: \"smartrecruiters\"" niches/wind-turbine.ts` returns >= 1 (>= 2 preferred per the must_have, but 1 acceptable if no second tenant verified — document in SUMMARY)
    - Each `companyId` has been verified live (manual curl probe before commit; logged in SUMMARY)
    - `pnpm tsc --noEmit` passes
    - `pnpm vitest run` (full suite) passes — including wave1.test.ts which checks Wave 1 state still intact
    - Section header comment added: `grep -q "Wave 2: SmartRecruiters" niches/wind-turbine.ts`
  </acceptance_criteria>
  <done>At least 1 SmartRecruiters tenant added (preferably 2); companyIds verified via curl; tsc green.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → SmartRecruiters API | Public endpoint, no auth — depends on SmartRecruiters server-side rate limiting |
| SmartRecruiters API → Worker | Untrusted JSON; structural fields used (id, name, location); no HTML in list response |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | Tampering | Malformed JSON crashes adapter | mitigate | Type assertion + try/catch in ingestSmartRecruiters; per-target failure isolated by Promise.allSettled |
| T-02-14 | Information Disclosure | companyId leak in error logs | accept | companyId is public (visible in jobs.smartrecruiters.com URL); no secrets exposed |
| T-02-15 | Denial of Service | Pagination loop runaway | mitigate | MAX_RECORDS = 1000 cap in adapter; offset increments by PAGE_SIZE |

</threat_model>

<verification>
- `pnpm vitest run` (full suite) passes
- `pnpm tsc --noEmit` clean
- Manual: in production after deploy, `wrangler tail owljobs-ingest` shows `[wind-turbine] ingest complete: ... pinged: N ...` with N > 0 for the SmartRecruiters employers
- Supabase: `SELECT employer_id, count(*) FROM wind_turbine.jobs WHERE id IN (SELECT id FROM wind_turbine.job_sources WHERE source = 'smartrecruiters') GROUP BY employer_id` returns ≥ 1 row
</verification>

<success_criteria>
- [ ] SmartRecruiters list adapter built, paginates via offset+limit, sets description=null
- [ ] Adapter export entry in package.json
- [ ] At least 1 (preferably 2) verified-live SmartRecruiters tenant in wind-turbine niche
- [ ] ingestSmartRecruiters wired with creation pings + expireMissingJobs
- [ ] upsertEmployer writes companyId to ats_site (so fetch-description.ts can read)
- [ ] All tests green; tsc green
</success_criteria>

<output>
After completion, create `.planning/phases/02-employer-breadth-seo/02-05-SUMMARY.md` documenting:
- Adapter API contract + pagination behavior
- List of SmartRecruiters tenants added (with curl probe results)
- Test results
- Cumulative employer count (target: 9 + N SmartRecruiters tenants → progress to 20)
</output>
