---
phase: 02-employer-breadth-seo
plan: 04
type: execute
wave: 2
depends_on: [02-03]
files_modified:
  - packages/niches/src/index.ts
  - workers/ingest/src/ingest.ts
  - workers/ingest/src/fetch-description.ts
  - workers/ingest/test/fetch-description.test.ts
autonomous: true
requirements: [SEO-02]
must_haves:
  truths:
    - "fetch-description.ts is committed to git (was untracked) — fresh-clone pnpm build succeeds"
    - "AtsTarget union in packages/niches/src/index.ts includes SmartRecruitersTarget, TrakstarTarget, AdzunaTarget, JSearchTarget"
    - "EmployerInput.atsType union in ingest.ts includes 'smartrecruiters', 'trakstar', 'adzuna', 'jsearch' (Pitfall 5)"
    - "ingest.ts exhaustive switch in ingestNiche has stub branches for all 4 new atsTypes (TS compile passes)"
    - "fetch-description.ts has a SmartRecruiters branch using detail endpoint (Pitfall 3)"
    - "fetch-description.test.ts asserts routing per ats_type"
  artifacts:
    - path: "packages/niches/src/index.ts"
      provides: "extended AtsTarget union with 4 new types"
      contains: "SmartRecruitersTarget"
    - path: "workers/ingest/src/fetch-description.ts"
      provides: "ats_type-aware description router (now tracked in git)"
      contains: "smartrecruiters"
    - path: "workers/ingest/test/fetch-description.test.ts"
      provides: "source-contract test for description routing"
      min_lines: 30
  key_links:
    - from: "packages/niches/src/index.ts"
      to: "AtsTarget union extension"
      via: "interface declarations + union member addition"
      pattern: "SmartRecruitersTarget"
    - from: "workers/ingest/src/ingest.ts"
      to: "exhaustive switch with stub branches"
      via: "if/else chain in ingestNiche"
      pattern: 'atsType === .smartrecruiters.'
    - from: "workers/ingest/src/fetch-description.ts"
      to: "SmartRecruiters detail-endpoint branch"
      via: "ats_type === smartrecruiters router"
      pattern: "smartrecruiters"
---

## Phase Goal

**As a** wind turbine technician browsing OwlJobs, **I want to** see full job descriptions for Workday, SuccessFactors, Recruitee, and SmartRecruiters listings, **so that** I can decide whether to apply without leaving the site.

<objective>
Three connected jobs in one plan, all touching `packages/niches/src/index.ts` and `workers/ingest/src/ingest.ts` — they MUST land together or TypeScript compile breaks (Pitfall 5):

1. SEO-02 — Commit `fetch-description.ts` (currently untracked despite being imported by `enrich.ts` and `classify.ts`). A fresh `pnpm install + pnpm build` will fail without this — Pitfall 6.

2. Type union extension — Add `SmartRecruitersTarget`, `TrakstarTarget`, `AdzunaTarget`, `JSearchTarget` to `packages/niches/src/index.ts` and extend `EmployerInput.atsType` in `ingest.ts` (Pitfall 5).

3. fetch-description.ts SmartRecruiters branch — Add a branch for `ats_type === "smartrecruiters"` that fetches the detail endpoint (Pitfall 3 — list endpoint omits descriptions).

Adding all 4 types together (with stubs in the ingest switch) prevents the cascading TS errors that would happen if added piecemeal across Plans 05/06/07.

Purpose:
- SEO-02: `fetch-description.ts` committed and tested → enrichment pipeline has a single source of truth
- Type union foundation enables Plans 05/06/07 to drop in real implementations without re-editing the union

Output:
- `packages/niches/src/index.ts` — 4 new interfaces, extended `AtsTarget` union
- `workers/ingest/src/ingest.ts` — `EmployerInput.atsType` extended; 4 stub branches added in `ingestNiche` switch
- `workers/ingest/src/fetch-description.ts` — committed to git; SmartRecruiters branch added (lazy-fetch detail endpoint)
- `workers/ingest/test/fetch-description.test.ts` — source-contract test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-employer-breadth-seo/02-CONTEXT.md
@.planning/phases/02-employer-breadth-seo/02-RESEARCH.md
@packages/niches/src/index.ts
@workers/ingest/src/ingest.ts
@workers/ingest/src/fetch-description.ts
@workers/ingest/src/enrich.ts
@workers/ingest/src/classify.ts
@.planning/phases/02-employer-breadth-seo/02-01-PLAN.md
@.planning/phases/02-employer-breadth-seo/02-03-PLAN.md
</context>

<interfaces_reference>
CURRENT AtsTarget union in packages/niches/src/index.ts line 52:

    export type AtsTarget = WorkdayTarget | SuccessFactorsTarget | GreenhouseTarget | RecruiteeTarget | SoftgardenTarget;

CURRENT EmployerInput type in ingest.ts lines 384-391:

    interface EmployerInput {
      name: string;
      atsType: "workday" | "greenhouse" | "successfactors" | "recruitee" | "softgarden" | "direct";
      atsTenant?: string;
      atsInstance?: string;
      atsSite?: string;
      careersUrl?: string;
    }

CURRENT JobInput.source already includes adzuna and jsearch (line 423) — keep as-is.

TARGET interfaces from RESEARCH.md lines 591-635 (full code provided in Task 2 below).
</interfaces_reference>

<tasks>

<task type="auto">
  <name>Task 1: Commit untracked fetch-description.ts (Pitfall 6)</name>
  <files>workers/ingest/src/fetch-description.ts</files>
  <read_first>
    - workers/ingest/src/fetch-description.ts (current untracked content)
    - workers/ingest/src/enrich.ts (imports fetchDescription — confirms file is required at runtime)
    - workers/ingest/src/classify.ts (also imports fetchDescription)
  </read_first>
  <action>
    The file `workers/ingest/src/fetch-description.ts` already exists on disk but is NOT tracked by git. It is imported by `workers/ingest/src/enrich.ts` line 3 and `workers/ingest/src/classify.ts` line 3. A fresh clone fails to build without it (Pitfall 6).

    Run from repo root:

        git add workers/ingest/src/fetch-description.ts

    Do NOT create a commit yet — Tasks 2-5 modify the file further; a single commit at end of Plan 04 is cleaner.

    If `git status` shows the file is already tracked, this task is a no-op — skip and proceed.
  </action>
  <verify>
    <automated>git ls-files --error-unmatch workers/ingest/src/fetch-description.ts</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files --error-unmatch workers/ingest/src/fetch-description.ts` exits 0 (file is tracked)
    - File still exists on disk: `test -f workers/ingest/src/fetch-description.ts`
    - File contents unchanged from current state at this point
  </acceptance_criteria>
  <done>fetch-description.ts staged in git; fresh clone build no longer breaks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend AtsTarget union in packages/niches/src/index.ts</name>
  <files>packages/niches/src/index.ts</files>
  <read_first>
    - packages/niches/src/index.ts (current state — lines 1-99)
    - .planning/phases/02-employer-breadth-seo/02-RESEARCH.md (lines 591-635 — exact interface signatures)
    - .planning/phases/02-employer-breadth-seo/02-CONTEXT.md (D-05 through D-10 — atsType naming locked)
  </read_first>
  <action>
    Add 4 new interfaces and extend the AtsTarget union in `packages/niches/src/index.ts`.

    Step A — Insert immediately AFTER the existing `SoftgardenTarget` interface (line 50) and BEFORE the `AtsTarget` union declaration. Paste this block:

        /** SmartRecruiters public Postings API (no auth). D-09: lazy-fetch — list endpoint omits descriptions. */
        export interface SmartRecruitersTarget {
          employer: string;
          atsType: "smartrecruiters";
          /** Company identifier used in the SmartRecruiters API path, e.g. "Vestas" */
          companyId: string;
          cronGroup?: CronGroup;
        }

        /** Trakstar (SPA — fragile per D-10). Token extracted from initial HTML; abort to aggregator if not parseable. */
        export interface TrakstarTarget {
          employer: string;
          atsType: "trakstar";
          /** Subdomain of hire.trakstar.com, e.g. "orsted" */
          companySlug: string;
          cronGroup?: CronGroup;
        }

        /** Adzuna search aggregator (D-05). Sentinel `employer` field — aggregator targets are per-query, not per-employer. */
        export interface AdzunaTarget {
          employer: string;
          atsType: "adzuna";
          /** ISO 3166-1 alpha-2 country code — Adzuna endpoint requires this */
          country: string;
          cronGroup?: CronGroup;
        }

        /** JSearch (RapidAPI) aggregator fallback (D-06). Sentinel `employer` field. */
        export interface JSearchTarget {
          employer: string;
          atsType: "jsearch";
          cronGroup?: CronGroup;
        }

    Step B — Replace the existing AtsTarget union (line 52) with:

        export type AtsTarget =
          | WorkdayTarget
          | SuccessFactorsTarget
          | GreenhouseTarget
          | RecruiteeTarget
          | SoftgardenTarget
          | SmartRecruitersTarget
          | TrakstarTarget
          | AdzunaTarget
          | JSearchTarget;

    Constraints:
    - Do NOT add any actual targets to a niche config in this task (Plans 05/06/07 do that)
    - Do NOT remove existing types or change their fields
    - The CronGroup type is already declared at line 2 — reuse it
  </action>
  <verify>
    <automated>grep -q "interface SmartRecruitersTarget" packages/niches/src/index.ts && grep -q "interface TrakstarTarget" packages/niches/src/index.ts && grep -q "interface AdzunaTarget" packages/niches/src/index.ts && grep -q "interface JSearchTarget" packages/niches/src/index.ts && grep -q "SmartRecruitersTarget" packages/niches/src/index.ts && grep -q "AdzunaTarget" packages/niches/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new interfaces declared: `grep -c "export interface \\(SmartRecruitersTarget\\|TrakstarTarget\\|AdzunaTarget\\|JSearchTarget\\)" packages/niches/src/index.ts` returns 4
    - AtsTarget union includes all 9 names — verify each appears in the union: `grep -A 12 "export type AtsTarget" packages/niches/src/index.ts | grep -q SmartRecruitersTarget` and same for TrakstarTarget, AdzunaTarget, JSearchTarget
    - SmartRecruitersTarget has `companyId: string`: `grep -A 8 "interface SmartRecruitersTarget" packages/niches/src/index.ts | grep -q "companyId: string"`
    - AdzunaTarget has `country: string`: `grep -A 8 "interface AdzunaTarget" packages/niches/src/index.ts | grep -q "country: string"`
    - At THIS step `pnpm tsc --noEmit` is EXPECTED TO FAIL because ingest.ts switch is not yet exhaustive — Task 3 fixes it
  </acceptance_criteria>
  <done>4 new interfaces + extended union landed; tsc fails as expected (Task 3 fixes it).</done>
</task>

<task type="auto">
  <name>Task 3: Add stub switch branches + extend EmployerInput.atsType in ingest.ts</name>
  <files>workers/ingest/src/ingest.ts</files>
  <read_first>
    - workers/ingest/src/ingest.ts (current state — note Plan 03 added buildPublicUrl + creation pings; the if/else chain in ingestNiche around lines 38-50 needs new branches)
    - packages/niches/src/index.ts (just-extended union)
  </read_first>
  <action>
    Two changes to `workers/ingest/src/ingest.ts`:

    Step A — Extend the EmployerInput.atsType union (line 386):

    From:

        atsType: "workday" | "greenhouse" | "successfactors" | "recruitee" | "softgarden" | "direct";

    To:

        atsType: "workday" | "greenhouse" | "successfactors" | "recruitee" | "softgarden" | "smartrecruiters" | "trakstar" | "adzuna" | "jsearch" | "direct";

    This unblocks Pitfall 5 — Plans 05/06/07 cannot pass `atsType: "smartrecruiters"` etc. to `upsertEmployer` without this.

    Step B — Add 4 stub branches to the if/else chain inside ingestNiche. After Plan 03 the chain looks like:

        if (target.atsType === "workday") {
          await ingestWorkday(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "greenhouse") {
          await ingestGreenhouse(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "successfactors") {
          await ingestSuccessFactors(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "recruitee") {
          await ingestRecruitee(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "softgarden") {
          await ingestSoftgarden(target, niche, db, localStats, saJson, budget);
        } else {
          console.log(`[ingest] skipping unknown atsType (not yet implemented)`);
        }

    Replace with:

        if (target.atsType === "workday") {
          await ingestWorkday(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "greenhouse") {
          await ingestGreenhouse(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "successfactors") {
          await ingestSuccessFactors(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "recruitee") {
          await ingestRecruitee(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "softgarden") {
          await ingestSoftgarden(target, niche, db, localStats, saJson, budget);
        } else if (target.atsType === "smartrecruiters") {
          // STUB — real implementation in Plan 05
          console.log(`[ingest] smartrecruiters adapter not yet implemented (Plan 05): ${target.employer}`);
        } else if (target.atsType === "trakstar") {
          // STUB — real implementation in Plan 07
          console.log(`[ingest] trakstar adapter not yet implemented (Plan 07): ${target.employer}`);
        } else if (target.atsType === "adzuna") {
          // STUB — real implementation in Plan 06
          console.log(`[ingest] adzuna adapter not yet implemented (Plan 06): ${target.employer}`);
        } else if (target.atsType === "jsearch") {
          // STUB — real implementation in Plan 06
          console.log(`[ingest] jsearch adapter not yet implemented (Plan 06): ${target.employer}`);
        } else {
          // Exhaustiveness guard: union is closed; this branch is unreachable but TS requires it.
          const _exhaustive: never = target;
          console.log(`[ingest] unreachable atsType:`, _exhaustive);
        }

    Constraints:
    - The 4 stub branches do NO DB writes — they only log. They exist solely to satisfy TS exhaustive-check.
    - Do NOT add upsertEmployer calls in stubs — Plans 05/06/07 do that with the correct adapter logic.
    - The `_exhaustive: never` line is the canonical TS exhaustiveness pattern; future additions caught at compile time.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit 2>&1 | tail -5 && grep -q "smartrecruiters" workers/ingest/src/ingest.ts && grep -q "trakstar" workers/ingest/src/ingest.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm tsc --noEmit` exits 0 (no errors) — Task 2 + Task 3 together unblock compilation
    - `ingest.ts` contains all 4 new atsType strings: `grep -q "smartrecruiters" workers/ingest/src/ingest.ts && grep -q "trakstar" workers/ingest/src/ingest.ts && grep -q "adzuna" workers/ingest/src/ingest.ts && grep -q "jsearch" workers/ingest/src/ingest.ts`
    - `EmployerInput.atsType` includes all new strings: `grep -A 1 "atsType:" workers/ingest/src/ingest.ts | grep -q "smartrecruiters" && grep -A 1 "atsType:" workers/ingest/src/ingest.ts | grep -q "trakstar"`
    - Exhaustiveness guard present: `grep -q "_exhaustive: never" workers/ingest/src/ingest.ts`
    - All existing tests still pass: `pnpm vitest run workers/ingest/test/` exits 0
  </acceptance_criteria>
  <done>EmployerInput union extended; 4 stub branches added; exhaustive check passes; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Add SmartRecruiters branch to fetch-description.ts</name>
  <files>workers/ingest/src/fetch-description.ts</files>
  <read_first>
    - workers/ingest/src/fetch-description.ts (current state — has Workday, SuccessFactors, Recruitee branches)
    - .planning/phases/02-employer-breadth-seo/02-RESEARCH.md (lines 290-345 — SmartRecruiters detail endpoint shape)
    - .planning/phases/02-employer-breadth-seo/02-CONTEXT.md (D-09 — SmartRecruiters lazy-fetch pattern)
    - packages/ats-adapters/src/sanitize.ts (sanitizeJobDescription helper — apply to fetched HTML)
    - packages/ats-adapters/src/workday.ts (lines 144-169 — fetchWorkdayJobDescription as reference pattern for lazy-fetch + sanitize)
  </read_first>
  <behavior>
    - Behavior 1: Given a row with `ats_type === "smartrecruiters"`, fetchDescription returns sanitized HTML by hitting the SmartRecruiters detail endpoint
    - Behavior 2: When the canonical_url is malformed or fetch fails, returns `null` (no exception thrown)
    - Behavior 3: Existing Workday/SuccessFactors/Recruitee branches still work (no regression)
    - Behavior 4: HTML is run through `sanitizeJobDescription` before returning (XSS guard)
  </behavior>
  <action>
    Add a new branch to `workers/ingest/src/fetch-description.ts` for `ats_type === "smartrecruiters"`. The SmartRecruiters detail endpoint requires the company ID (which is in `ats_site` after Plan 05 wires `upsertEmployer` to write companyId there) and the posting ID.

    Per Pitfall 3, the detail endpoint URL is `https://api.smartrecruiters.com/v1/companies/{companyId}/postings/{postingId}` and returns `{ jobAd: { sections: { jobDescription: { text: "<html>" }, qualifications: { text: "<html>" }, ... } } }`.

    The canonical_url for SmartRecruiters jobs (set by the adapter in Plan 05) will be the public posting URL `https://jobs.smartrecruiters.com/{companyId}/{postingId}`. Extract `companyId` and `postingId` from it.

    Insert this branch BEFORE the final `return null;` (which serves as the Greenhouse/softgarden fallthrough comment):

        if (ats_type === "smartrecruiters" && ats_site) {
          // canonical_url shape (set by SmartRecruiters adapter in Plan 05):
          //   https://jobs.smartrecruiters.com/{companyId}/{postingId}
          // ats_site stores companyId for SmartRecruiters employers.
          // Extract postingId by splitting the canonical URL.
          const match = row.canonical_url.match(/jobs\.smartrecruiters\.com\/[^/]+\/([^/?#]+)/);
          if (!match) return null;
          const postingId = match[1];
          const apiUrl = `https://api.smartrecruiters.com/v1/companies/${ats_site}/postings/${postingId}`;

          try {
            const resp = await fetch(apiUrl, { headers: { Accept: "application/json" } });
            if (!resp.ok) return null;
            const data = (await resp.json()) as {
              jobAd?: {
                sections?: {
                  jobDescription?: { text?: string };
                  qualifications?: { text?: string };
                  additionalInformation?: { text?: string };
                };
              };
            };
            const sections = data.jobAd?.sections ?? {};
            const parts = [
              sections.jobDescription?.text,
              sections.qualifications?.text,
              sections.additionalInformation?.text,
            ].filter(Boolean) as string[];
            if (parts.length === 0) return null;
            const { sanitizeJobDescription } = await import("@owljobs/ats-adapters/sanitize");
            return sanitizeJobDescription(parts.join("\n\n"));
          } catch {
            return null;
          }
        }

    Constraints:
    - Use `await import(...)` for the sanitize helper to avoid adding a top-level import (keeps module loading lazy if SmartRecruiters branch never runs).
    - Sanitize before returning (matches workday.ts pattern line 165).
    - `ats_site` stores the SmartRecruiters companyId (Plan 05 will wire upsertEmployer to write it there). If `ats_site` is null, return null silently — the caller (enrich.ts) treats null as "skip this row".
    - Do NOT touch the existing Workday/SuccessFactors/Recruitee branches.
  </action>
  <verify>
    <automated>grep -q "smartrecruiters" workers/ingest/src/fetch-description.ts && grep -q "api.smartrecruiters.com" workers/ingest/src/fetch-description.ts && grep -q "sanitizeJobDescription" workers/ingest/src/fetch-description.ts && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains `smartrecruiters` branch: `grep -c "smartrecruiters" workers/ingest/src/fetch-description.ts` returns >= 2 (one for the comparison, one for the URL)
    - File references the SmartRecruiters API host: `grep -q "api.smartrecruiters.com/v1/companies" workers/ingest/src/fetch-description.ts`
    - Sanitization is applied: `grep -q "sanitizeJobDescription" workers/ingest/src/fetch-description.ts`
    - Function still returns `string | null` (signature unchanged): `grep -q "Promise<string | null>" workers/ingest/src/fetch-description.ts`
    - `pnpm tsc --noEmit` passes
  </acceptance_criteria>
  <done>SmartRecruiters branch added; sanitization applied; tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Wave 0 — fetch-description.test.ts source-contract test</name>
  <files>workers/ingest/test/fetch-description.test.ts</files>
  <read_first>
    - workers/ingest/test/upsert.test.ts (source-contract pattern — read file as string + grep regex)
    - workers/ingest/src/fetch-description.ts (just-modified state — should now have 4 ats_type branches)
  </read_first>
  <behavior>
    - Test 1: fetch-description.ts contains a routing branch for ats_type === "workday"
    - Test 2: contains routing branch for ats_type === "successfactors"
    - Test 3: contains routing branch for ats_type === "recruitee"
    - Test 4: contains routing branch for ats_type === "smartrecruiters"
    - Test 5: SmartRecruiters branch references the api.smartrecruiters.com detail endpoint (Pitfall 3)
    - Test 6: SmartRecruiters branch applies sanitizeJobDescription
    - Test 7: file ends with a `return null;` (Greenhouse/softgarden fallthrough)
    - Test 8: file is tracked in git (Pitfall 6)
  </behavior>
  <action>
    Create `workers/ingest/test/fetch-description.test.ts` using the source-contract pattern.

    Exact content:

        import { describe, it, expect, beforeAll } from "vitest";
        import { readFile } from "node:fs/promises";
        import { execSync } from "node:child_process";

        describe("fetch-description.ts — ats_type routing contract (SEO-02, D-20, Pitfalls 3+6)", () => {
          let src = "";
          beforeAll(async () => {
            src = await readFile("workers/ingest/src/fetch-description.ts", "utf-8");
          });

          it("is tracked in git (Pitfall 6 — was untracked, must be committed)", () => {
            const out = execSync("git ls-files workers/ingest/src/fetch-description.ts", { encoding: "utf-8" }).trim();
            expect(out).toBe("workers/ingest/src/fetch-description.ts");
          });

          it("routes ats_type === \"workday\" to fetchWorkdayJobDescription", () => {
            expect(src).toMatch(/ats_type\s*===\s*["']workday["']/);
            expect(src).toMatch(/fetchWorkdayJobDescription/);
          });

          it("routes ats_type === \"successfactors\" to fetchSuccessFactorsJobDescription", () => {
            expect(src).toMatch(/ats_type\s*===\s*["']successfactors["']/);
            expect(src).toMatch(/fetchSuccessFactorsJobDescription/);
          });

          it("routes ats_type === \"recruitee\" to fetchRecruiteeJobDescription", () => {
            expect(src).toMatch(/ats_type\s*===\s*["']recruitee["']/);
            expect(src).toMatch(/fetchRecruiteeJobDescription/);
          });

          it("routes ats_type === \"smartrecruiters\" to detail endpoint (Pitfall 3)", () => {
            expect(src).toMatch(/ats_type\s*===\s*["']smartrecruiters["']/);
            expect(src).toMatch(/api\.smartrecruiters\.com\/v1\/companies/);
          });

          it("SmartRecruiters branch applies sanitizeJobDescription before returning", () => {
            const idx = src.search(/ats_type\s*===\s*["']smartrecruiters["']/);
            expect(idx).toBeGreaterThan(-1);
            // Slice forward — the branch body should contain sanitizeJobDescription
            const branch = src.slice(idx, idx + 1500);
            expect(branch).toMatch(/sanitizeJobDescription/);
          });

          it("falls through with return null for unsupported ats_type (greenhouse/softgarden)", () => {
            // The last few lines should be a return null fallthrough comment + return statement
            expect(src).toMatch(/return null;\s*\n?\}\s*$/);
          });
        });
  </action>
  <verify>
    <automated>pnpm vitest run workers/ingest/test/fetch-description.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f workers/ingest/test/fetch-description.test.ts`
    - All 7 tests pass: `pnpm vitest run workers/ingest/test/fetch-description.test.ts` exits 0
    - Test file uses source-contract pattern: `grep -q "from .node:fs/promises." workers/ingest/test/fetch-description.test.ts`
    - Tests cover all 4 atsType branches plus the git-tracked check
  </acceptance_criteria>
  <done>fetch-description.test.ts created; all 7 tests pass; routing contract verified.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker → SmartRecruiters API | Public endpoint, no auth; rate limited by SmartRecruiters server-side |
| SmartRecruiters API → Worker | Untrusted HTML in jobAd.sections — sanitized before DB write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Tampering | XSS via SmartRecruiters HTML | mitigate | sanitizeJobDescription applied to all parts before return (workers/ingest/src/fetch-description.ts SmartRecruiters branch) |
| T-02-11 | Information Disclosure | Malformed canonical_url leaks via fetch | accept | URL is from our own DB (set by adapter); regex match guard prevents arbitrary fetch |
| T-02-12 | Denial of Service | SmartRecruiters API timeout hangs enrich worker | accept | Per-row try/catch in enrich.ts isolates failures; `Promise.allSettled` chunk loop limits concurrency to 8 |

</threat_model>

<verification>
- `git ls-files workers/ingest/src/fetch-description.ts` returns the path (file tracked)
- `pnpm tsc --noEmit` passes
- `pnpm vitest run workers/ingest/test/` (full directory) passes
- Manual: a job with ats_type=smartrecruiters in the DB enriches successfully on next cron (deferred to Plan 05 when first SmartRecruiters target is activated)
</verification>

<success_criteria>
- [ ] fetch-description.ts tracked in git
- [ ] 4 new AtsTarget interfaces declared in packages/niches/src/index.ts
- [ ] AtsTarget union includes 9 members
- [ ] EmployerInput.atsType in ingest.ts includes 4 new strings
- [ ] ingest.ts has 4 stub branches with `_exhaustive: never` guard
- [ ] fetch-description.ts has SmartRecruiters branch with sanitization
- [ ] fetch-description.test.ts exists and 7 tests pass
- [ ] `pnpm tsc --noEmit` clean
- [ ] No regressions: `pnpm vitest run workers/ingest/test/` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-employer-breadth-seo/02-04-SUMMARY.md` documenting:
- Files modified (4)
- Type union additions (4 new interfaces)
- fetch-description routing table (ats_type → handler)
- Test results
</output>
